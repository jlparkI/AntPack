#include "numbering_utilities.h"


// Handles the slightly ticklish business of converting another
// numbering scheme to IMGT. Much of this is hard-coded, which
// would ordinarily be non-ideal, but...the numbering schemes
// are ancient and unlikely to change in the forseeable future.
void convert_numbering_to_imgt(std::vector<std::string> &og_numbering,
        std::vector<std::string> &con_numbering,
        std::string &scheme){
}
